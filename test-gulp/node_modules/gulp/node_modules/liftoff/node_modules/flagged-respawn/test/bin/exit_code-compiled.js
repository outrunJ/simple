#!/usr/bin/env node
'use strict';

var flaggedRespawn = require('../../');

flaggedRespawn(['--harmony'], process.argv, function (ready) {

  if (ready) {
    setTimeout(function () {
      process.exit(100);
    }, 100);
  }
});

//# sourceMappingURL=exit_code-compiled.js.map