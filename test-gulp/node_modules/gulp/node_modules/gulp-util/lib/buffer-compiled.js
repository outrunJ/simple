'use strict';

var through = require('through2');

module.exports = function (fn) {
  var buf = [];
  var end = function end(cb) {
    this.push(buf);
    cb();
    if (fn) fn(null, buf);
  };
  var push = function push(data, enc, cb) {
    buf.push(data);
    cb();
  };
  return through.obj(push, end);
};

//# sourceMappingURL=buffer-compiled.js.map